const buy = document.getElementById("buy")

const mydiv = document.getElementById("mydiv");


buy.addEventListener("click", 

    (eo) => {

        eo.target

        eo.target.classList.add("bc")

    }

)
